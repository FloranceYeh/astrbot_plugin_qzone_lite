{
  "cookies_str": {
    "description": "QQ Cookies",
    "type": "text",
    "hint": "选填，用于维持 QQ 空间登录态；留空则自动从 aiocqhttp 客户端获取",
    "default": ""
  },
  "send_feedback": {
    "description": "发送反馈",
    "type": "bool",
    "hint": "是否在执行操作时返回反馈信息",
    "default": true
  },
  "analyze_images_on_view_feed": {
    "description": "查看说说时分析图片",
    "type": "bool",
    "hint": "开启后，查看说说/LLM 查看说说时，如果说说包含图片，会调用视觉模型分析图片并把结果追加到返回内容中",
    "default": false
  },
  "vision_provider_id": {
    "description": "图片分析视觉模型",
    "type": "string",
    "hint": "请选择支持图片输入的模型提供商；留空或配置无效时使用当前正在使用的模型提供商",
    "default": "",
    "_special": "select_provider"
  },
  "vision_prompt": {
    "description": "图片分析提示词",
    "type": "text",
    "hint": "用于指导视觉模型如何分析说说图片",
    "default": "请分析这条 QQ 空间说说中的图片内容，用简洁中文概括图片里可见的主体、场景、文字和可能表达的情绪。不要编造看不见的信息。"
  },
  "feed_cache_max_size": {
    "description": "说说缓存数量",
    "type": "int",
    "hint": "最多缓存多少条已查询到的说说内容；设为 0 可关闭缓存",
    "slider": { "min": 0, "max": 500, "step": 1 },
    "default": 50
  },
  "feed_cache_ttl_seconds": {
    "description": "说说缓存时间（秒）",
    "type": "int",
    "hint": "缓存有效期。有效期内再次查询相同位置的说说时复用正文、图片和图片分析，只刷新评论内容；设为 0 可关闭缓存",
    "slider": { "min": 0, "max": 86400, "step": 60 },
    "default": 1800
  },
  "timeout": {
    "description": "请求超时时间",
    "type": "int",
    "hint": "网络请求超时时间（秒）",
    "slider": { "min": 5, "max": 60, "step": 1 },
    "default": 10
  },
  "request_interval": {
    "description": "QQ空间请求最小间隔（秒）",
    "type": "float",
    "hint": "每次请求前至少等待该时长，降低限流概率",
    "slider": { "min": 0, "max": 5, "step": 0.1 },
    "default": 0.8
  },
  "request_jitter": {
    "description": "QQ空间请求随机抖动（秒）",
    "type": "float",
    "hint": "在最小间隔基础上增加 0~该值的随机延时",
    "slider": { "min": 0, "max": 3, "step": 0.1 },
    "default": 0.6
  }
}
